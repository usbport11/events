#include "logic\processor.h"

int main() {
    //MProcessor* processor = new MProcessor;
    //processor->start();
    //processor->resolveEvents();
    //if(processor) delete processor;

    return 0;
}
